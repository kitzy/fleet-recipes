# ==============================================================================
# FleetImporter AutoPkg Processor Configuration
# ==============================================================================
# 
# This file shows example environment variables for the FleetImporter processor.
# The processor checks for values in this order:
#   1. Environment variables
#   2. AutoPkg preferences (com.github.autopkg plist)
#   3. Recipe input variables
#
# RECOMMENDED: Use AutoPkg preferences for persistent configuration:
#   defaults write com.github.autopkg AWS_ACCESS_KEY_ID "your-key"
#   defaults write com.github.autopkg AWS_SECRET_ACCESS_KEY "your-secret"
#   defaults write com.github.autopkg AWS_DEFAULT_REGION "us-east-1"
#   etc.
#
# ALTERNATIVE: Copy this file to .env and source it before running AutoPkg:
#   cp .env.example .env
#   # Edit .env with your actual values
#   export $(grep -v '^#' .env | xargs)
#   autopkg run YourRecipe.fleet.recipe.yaml
#
# ==============================================================================

# ==============================================================================
# Fleet API Configuration (required for DIRECT mode)
# ==============================================================================
FLEET_API_BASE=https://fleet.example.com
FLEET_API_TOKEN=your-fleet-api-token-here
FLEET_TEAM_ID=1

# ==============================================================================
# AWS Configuration (required for GITOPS mode)
# ==============================================================================
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# S3 bucket for package storage
AWS_S3_BUCKET=your-fleet-packages-bucket

# CloudFront distribution domain (without https://)
AWS_CLOUDFRONT_DOMAIN=d1234567890abc.cloudfront.net

# ==============================================================================
# GitOps Configuration (required for GITOPS mode)
# ==============================================================================
# GitHub repository for GitOps YAML files
FLEET_GITOPS_REPO_URL=https://github.com/org/fleet-gitops.git
FLEET_GITOPS_GITHUB_TOKEN=your-github-personal-access-token

# Paths within the GitOps repository
FLEET_GITOPS_SOFTWARE_DIR=lib/macos/software
FLEET_GITOPS_TEAM_YAML_PATH=teams/team-name.yml

# S3 retention (optional, defaults to 3)
S3_RETENTION_VERSIONS=3
